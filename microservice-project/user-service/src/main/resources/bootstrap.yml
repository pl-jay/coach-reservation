server:
  port: 0

eureka:
  client:
    enabled: true
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: {http://localhost:8761/eureka/:http://discovery-service:8761/eureka/}
    instance:
      hostname: localhost

spring:
  application:
    name: user-service